{
  "name": "esp-game",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "npm run install-client && npm run install-server",
    "install-client": "cd client && npm install",
    "install-server": "cd server && npm install",
    "build-client": "cd client && npm run build",
    "build-backend-start": "cd server && npm run build && npm run start",
    "start": "npm run build-client && npm run build-backend-start"
  },
  "author": "harish",
  "license": "ISC"
}
